<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dishcovery.project.persistence.ProductMapper">

    <!-- 상품 등록 -->
	<insert id="insertProduct">
	    INSERT INTO Products (product_id, product_name, product_price, product_description, ingredient_id, product_image_path, stock, created_at, updated_at)
	    VALUES (PRODUCT_SEQ.NEXTVAL, #{productName}, #{productPrice}, #{productDescription}, #{ingredientId}, #{productImagePath, jdbcType=VARCHAR}, #{stock}, SYSDATE, SYSDATE)
	</insert>

    <!-- Ingredients 목록 조회 -->
    <select id="getAllProductIngredients" resultType="com.dishcovery.project.domain.IngredientsVO">
        SELECT ingredient_id, ingredient_name FROM Ingredients
    </select>

</mapper>
